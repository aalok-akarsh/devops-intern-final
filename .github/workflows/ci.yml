name: CI

on:
  push:
    branches:
      - main
      - "**"

jobs:
  run-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run hello.py
        run: python hello.py